// @flow

import variable from "./../variables/platform";

export default (variables /*: * */ = variable) => {
  const linericonTheme = {
    fontSize: variables.iconFontSize,
    color: "#000"
  };

  return linericonTheme;
};
